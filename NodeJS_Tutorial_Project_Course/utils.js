const name1 = 'Maria';
const name2 = 'Juan'

const basicOperation = (num1, num2) => {
    return num1 + num2;
}

// Podemos exportar ya sea una funcion, variable, objeto etc....
module.exports = { name1, name2, basicOperation }